# Recommended Screenshots

Add these images after running the application:

1. `streamlit_home.png` — title, code input, responsible-use warning
2. `generated_comment.png` — generated comment, latency, decoder, baseline comparison
3. `attention_heatmap.png` — only after retraining the true attention model
4. `training_results.png` — training curve and audited metrics
5. `repository_structure.png` — clean GitHub folder view

Do not publish a screenshot that labels the supplied legacy checkpoint as an attention model.
