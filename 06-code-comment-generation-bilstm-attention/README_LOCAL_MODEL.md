# Local Model Note

The included model was saved with Keras 3.14.0. Use Python 3.11 and install the runtime requirements. If deserialization fails because of a local TensorFlow/Keras mismatch, create a fresh environment and reinstall from `requirements.txt`. Loading uses `compile=False`, so the original optimizer state and unmasked metric are not required for inference.
