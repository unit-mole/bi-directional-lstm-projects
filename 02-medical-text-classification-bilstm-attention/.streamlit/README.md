# Streamlit configuration

This monorepo uses the shared configuration at:

```text
bi-directional-lstm-projects/.streamlit/config.toml
```

A second `config.toml` is intentionally not duplicated here because Streamlit
Community Cloud expects one repository-level configuration for a monorepo.
