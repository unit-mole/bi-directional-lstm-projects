# Archive

This folder preserves the original uploaded notebook before modularization:

`Medical_Text_Classification_using_Bi_Directional_LSTM_COMPLETE_corrected.ipynb`

Use the cleaned notebook in `notebooks/` and the reusable code in `src/` for future development. The archive is retained for traceability and before/after comparison.
